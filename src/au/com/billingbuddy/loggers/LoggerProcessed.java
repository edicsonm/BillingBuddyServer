package au.com.billingbuddy.loggers;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

public class LoggerProcessed {
	public static final Logger logger = LogManager.getLogger(LoggerProcessed.class);
}
